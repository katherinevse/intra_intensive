//Create a macro ABS which replaces its argument by it absolute value 

#ifndef FT_ABS_H
#define FT_ABS_H

#define ABS(x) ((x) < 0 ? -(x) : (x)) //логика вычисления абсолютного значения числа

#endif
